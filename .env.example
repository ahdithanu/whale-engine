# --- pipeline: external data sources for ingest.py ---
# Add one var per API the ingestion step calls, e.g.:
DATA_PROVIDER_API_KEY=
GEOCODING_API_KEY=

# --- pipeline: local analytical store ---
DUCKDB_PATH=./data/facility_intel.duckdb

# --- web: Next.js app ---
# Base URL the app reads facility data from, if not using the local JSON export.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
